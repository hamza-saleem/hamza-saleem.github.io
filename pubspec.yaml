name: portfolio_website
description: "Hamza Saleem — Personal portfolio website."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.0
  url_launcher: ^6.3.0
  flutter_bloc: ^9.1.0
  font_awesome_flutter: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  bloc_test: ^10.0.0
  test: ^1.26.2

flutter:
  uses-material-design: true
