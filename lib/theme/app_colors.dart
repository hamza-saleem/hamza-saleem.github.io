import 'package:flutter/material.dart';

class KageMichiColors {
  KageMichiColors._();

  // Dark mode
  static const darkBackground = Color(0xFF1A1A1F);
  static const darkSurface = Color(0xFF0F0F14);
  static const darkCard = Color(0xFF22222A);
  static const darkRule = Color(0xFF2A2A30);
  static const darkTextPrimary = Color(0xFFF5F0EB);
  static const darkTextSecondary = Color(0xFF9A9A8A);

  // Light mode
  static const lightBackground = Color(0xFFF5F0E8);
  static const lightSurface = Color(0xFFEDE8DC);
  static const lightCard = Color(0xFFE4DDD0);
  static const lightRule = Color(0xFFD0C8B8);
  static const lightTextPrimary = Color(0xFF1A1A1F);
  static const lightTextSecondary = Color(0xFF4A4A40);

  // Shared
  static const crimson = Color(0xFFC0392B);
  static const crimsonLight = Color(0xFFE74C3C);
}
