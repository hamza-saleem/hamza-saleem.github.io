import 'package:flutter/material.dart';

class KageMichiColors {
  KageMichiColors._();

  // Dark mode — deep warm blacks, like the logo's shadowed background
  static const darkBackground = Color(0xFF141412);
  static const darkSurface = Color(0xFF0F0E0C);
  static const darkCard = Color(0xFF1C1A16);
  static const darkRule = Color(0xFF28261E);
  static const darkTextPrimary = Color(0xFFEDE5D8);
  static const darkTextSecondary = Color(0xFF8A8070);

  // Light mode — warm cream tones for contrast with dark mode
  static const lightBackground = Color(0xFFF5F0E8);
  static const lightSurface = Color(0xFFEDE8DC);
  static const lightCard = Color(0xFFE4DDD0);
  static const lightRule = Color(0xFFD0C8B8);
  static const lightTextPrimary = Color(0xFF1C1A14);
  static const lightTextSecondary = Color(0xFF5A5448);

  // Accent — sandy beige drawn from the logo's hooded figure
  // Dark mode: bright enough to pop on charcoal backgrounds
  static const sand = Color(0xFFC4B49A);
  static const sandHover = Color(0xFFD4C4AA);
  // Light mode: darkened for legible contrast on cream backgrounds
  static const sandDark = Color(0xFF7A6A52);
  static const sandDarkHover = Color(0xFF8A7A62);
}
